// netlify/functions/package.json
{
  "name": "netlify-functions",
  "version": "1.0.0",
  "description": "Dependencies for Netlify serverless functions",
  "main": "proxy.js", // Ensure this points to your main function file
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-fetch": "^2.6.7" // Using a slightly older, stable version that is known to work well
  }
}
